@model Empleados
@{
    ViewData["Title"] = "Eliminar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Eliminar</h1>

<div class="card">
    <div class="card-header">
        Eliminar informacion de empleados
    </div>
    <div class="card-body">
        <form asp-action="Eliminar" asp-controller="Empleados" method="post">
            <input asp-for="EMPLEADOS_CODIGO" type="hidden" class="form-control" />

            <div class="alert alert-danger" role="alert">
                ¿Desea eliminar el contacto: @Html.DisplayTextFor(m => m.NOMBRE)?
            </div>

            <button type="submit" class="btn btn-primary" asp-action="Eliminar" asp-controller="Empleados">Eliminar</button>
            <a asp-action="Listar" asp-controller="Empleados" class="btn btn-warning">Volver al listado</a>
        </form>
    </div>
</div>
